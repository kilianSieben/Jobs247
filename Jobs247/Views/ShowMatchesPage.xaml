<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Jobs247;assembly=Jobs247"
             x:Class="Jobs247.Views.ShowMatchesPage"
             Title="{x:Static local:Constants.HeaderTitle}">
    <ContentPage.Content>
        <ListView x:Name="MatchingJobsListView" ItemSelected="OnMatchingJobsItemClicked">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding JobTitle}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>